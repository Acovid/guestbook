**Assignment - Guestbook app**

This app deploys the sample voting app, Cats and Dogs via deployments.
To deploy this app on Kubernetes cluster, do following:

1. From the command line which is connected to Kubernetes cluster (cloud or Minikube), clone the app from Github: *git clone https://github.com/Acovid/guestbook.git*
2. Move to the directory with the code: *cd kubernetes-example-voting-app*
3. Create Kubernetes deployment by running this command:
   *kubectl create -f .*
